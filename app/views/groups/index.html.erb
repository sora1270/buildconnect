<h3>参加中ユーザー</h3>
<% if @members.any? %>
  <ul>
    <% @members.each do |participant| %>
      <li>
        <%= link_to participant.full_name, user_path(participant) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>参加中のユーザーはいません。</p>
<% end %>