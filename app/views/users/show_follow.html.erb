<% provide(:title, @title) %>
<h1><%= @title %></h1>
<ul class="list-group">
  <% @users.each do |user| %>
    <li class="list-group-item">
      <%= link_to user.full_name, user_path(user) %>
    </li>
  <% end %>
</ul>